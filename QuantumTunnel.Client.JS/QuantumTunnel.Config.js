var QT = (function (qt) {

    qt.config = {
        log: {
            connectionEvents: true,
            internalEvents: true,
            tunnelingEvents:true
        }
    };

    return qt;
}(QT || {}));